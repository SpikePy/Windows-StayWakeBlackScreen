name: Build and Release

on:
  push:
    tags: ['v*']
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up Go
        uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
          cache-dependency-path: go.sum

      - name: Test
        run: go test ./...

      - name: Vet
        # -unsafeptr=false: this package's unsafe.Pointer conversions of
        # LPARAM/DIB-section addresses supplied by Win32 (hook callbacks,
        # CreateDIBSection's output buffer) are outside anything the Go
        # object graph tracks, which is inherent to raw Win32 interop, not
        # a bug - vet's static heuristic can't tell that apart from a real
        # misuse.
        run: go vet -unsafeptr=false ./...
        env:
          GOOS: windows
          GOARCH: amd64

      - name: Build StayWakeBlackScreen.exe
        run: go build -trimpath -ldflags "-H=windowsgui -s -w -X main.version=${{ github.ref_name }}" -o StayWakeBlackScreen.exe ./cmd/staywakeblackscreen
        env:
          GOOS: windows
          GOARCH: amd64
          CGO_ENABLED: 0

      - name: Build Setup_StayWakeBlackScreen.exe
        run: go build -trimpath -ldflags "-H=windowsgui -s -w -X main.version=${{ github.ref_name }}" -o Setup_StayWakeBlackScreen.exe ./cmd/stay-wake-setup
        env:
          GOOS: windows
          GOARCH: amd64
          CGO_ENABLED: 0

      - name: Upload build artifacts
        uses: actions/upload-artifact@v7
        with:
          name: StayWakeBlackScreen-exe
          path: |
            StayWakeBlackScreen.exe
            Setup_StayWakeBlackScreen.exe

      - name: Create GitHub Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v3
        with:
          files: |
            StayWakeBlackScreen.exe
            Setup_StayWakeBlackScreen.exe
          generate_release_notes: true
